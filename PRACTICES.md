# Practices

The list. Updated once a week, at the weekly review ([METHOD.md](METHOD.md) §6).

**Active: 0 of 3** — data collection has not started.

---

## Active

*Nothing yet. A practice gets here only after its friction happened three times across three
sessions, and only if there is a free slot.*

Format:

```markdown
### P001 — <name, one line, starts with a verb>

- **Targets:** <friction type>
- **Based on:** <the session ids where it happened 3+ times>
- **The rule:** <what you actually do — clear enough that you can tell later if you did it>
- **Wrong if:** <which friction should stop, and by when>
- **Started:** YYYY-MM-DD
- **Judge after:** YYYY-MM-DD (at least 14 days and 10 sessions)
- **Follow-through:** used N / missed N
```

---

## Waiting

Candidates that hit the 3-in-3 line but have no free slot. In order — the top one takes the
next slot that opens.

*Empty.*

---

## Finished

Practices whose window is over. Dropped ones stay here with the same space as the ones that
worked. A list that only shows wins is advertising, not measurement.

| ID | Practice | Targets | Result | Window | Note |
|---|---|---|---|---|---|
| — | — | — | — | — | — |

---

## Friction so far

Running count, updated at the weekly review. A type that hits **3 times across 3 different
sessions** becomes a candidate.

| Type | Times | Different sessions | Status |
|---|---|---|---|
| `rework` | 0 | 0 | below the line |
| `correction` | 0 | 0 | below the line |
| `misdirection` | 0 | 0 | below the line |
| `loop` | 0 | 0 | below the line |
| `overreach` | 0 | 0 | below the line |
| `regression` | 0 | 0 | below the line |
| `blind-spot` | 0 | 0 | below the line |
| `unclassified` | 0 | 0 | — |

*`unclassified` hitting 3 means you probably need a new type. Add it by amendment, not by
editing this table.*
